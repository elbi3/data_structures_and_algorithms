import { sort } from './solution.js';

describe('sort', () => {
  it.todo('sorts a small array of integers');
  it.todo('sorts a large array of integers');
  it.todo('sorts a nearly sorted array of integers');
  it.todo('sorts a reversed-order array of integers (descending order)');
  it.todo('sorts an empty array and an array with one element (edge case)');
  it.todo('handles a single-element array (edge case)');

  //  delete the it.todo lines as you replace them.
});